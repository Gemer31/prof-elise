export enum ButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit'
}

export enum RouterPath {
  MAIN = '/',
  DELIVERY = '/delivery',
  CONTACTS = '/contacts',
  LOGIN = '/login',
  EDITOR = '/editor',
}